// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.ssl.browser"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
